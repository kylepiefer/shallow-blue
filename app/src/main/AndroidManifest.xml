<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shallowblue.shallowblue">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainMenu"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameBoard" />
        <activity android:name=".MultiplayerSettings" />
        <activity android:name=".SingleplayerSettings" />
        <activity android:name=".Options_Settings_MAIN" />
        <activity android:name=".PVPGameBoard" />
        <activity android:name=".CustomGameBoard" />
        <activity android:name=".loadgame"></activity>
    </application>

</manifest>
